short term:
	exceptions.hpp
		figure out how to print stack trace in C++
	cdsp::circular_buffer
	parameters overhaul (see bottom) (circular buffer)
	primitives overhaul:
		all of these for rate_block<T> vs rate_linear:
			parameter<T>& parameter_get_read(specifier)
			const parameter<T>& parameter_get_write(specifier)
			void parameter_register_read(specifier, const parameter&)
			void parameter_register_write(specifier, parameter&)
	envelopes overhaul (align with parameter) (can this be the same code as rate_audio?)
	make interpolate framework a templated parameter to any table oscillator <interpolate::four, interpolate::two, interpolate::truncate>
	graph.hpp
	create README.md

long term:
	remove pluralism on namespace/folder names
	move hpp stuff to cpp
	cmake project for cross platform (library for static linking)
	test on mac, linux, ARM

optimizations:
	phasor.hpp and table_hpp UNITBIT32

bugfixes:
	hard clip input for table_function

questions:
	figure out using for constructors
	initialization list used by all constructors?
	shared_ptr for shared tables? (table_interpolate_4)

parameter:
	//https://developer.mozilla.org/en-US/docs/Web/API/AudioParam except with relative times instead of absolute
	parameter<T> (need to add all functionality here then throw exceptions)
			setValueAtTime(value, time)
			linearRampToValueAtTime(
		rate_block<T>
		rate_audio
			signal